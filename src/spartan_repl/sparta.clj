(ns spartan-repl.sparta)

(comment
  ;; in a fresh REPL...
  ;; via clojure -A:socket
  ;; sparta=> madness?
  ;; Syntax error compiling at (REPL:0:0).
  ;; Unable to resolve symbol: madness? in this context
  (def madness? :THIS-IS-SPARTA!)
  )
